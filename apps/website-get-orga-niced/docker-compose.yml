services:
  website-get-orga-niced:
    image: nginxinc/nginx-unprivileged:alpine
    container_name: website-get-orga-niced

    user: "${PUID}:${GUID}"
    restart: unless-stopped

    env_file:
      - ../../.env
      - .env

    cap_drop:
      - ALL

    security_opt:
      - no-new-privileges:true

    read_only: true

    tmpfs:
      - /tmp
      - /var/cache/nginx
      - /var/run

    volumes:
      - ${WEB_CONTENT_PATH}:/usr/share/nginx/html:ro

    networks:
      - traefik-public
    
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.web-get-orga-niced.rule=${HOST_RULE}"
      - "traefik.http.routers.web-get-orga-niced.entrypoints=websecure"
      - "traefik.http.routers.web-get-orga-niced.tls.certresolver=myresolver"
      - "traefik.http.routers.web-get-orga-niced.middlewares=sec-base@file"
      - "traefik.http.services.web-get-orga-niced.loadbalancer.server.port=8080"

networks:
  traefik-public:
    external: true